<h1>Multi-rider session Details</h1>
<h2><%= showday(@slot.date) %> Session at <%= @slot.start_time %></h2>


<div class="container">
  <div class="span10 offset1">
    <table class="table table-condensed table-bordered table-striped">
      <thead>
        <th>Rider Number</th>
        <th>Name</th>
        <th>Confirmed</th>
    </thead>
      <% @riders.each_with_index do |r, i| %>
          <tr>
              <td><%= i + 1 %></td>
              <td><%= r.first_name %> <%= r.last_name %></td>
              <td>#</td>
          </tr>
      <% end %>
    </table>

    <%= form_for @slot do |u|%>
        <%= u.submit "Join Session", class: "btn btn-large btn-primary" %>
        <%= u.submit "Leave Session", class:"btn btn-large btn-primary"  %>
    <% end %>

  </div>
</div>